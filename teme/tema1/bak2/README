--- Tema 1 SO - Multi-platform Development ---
--- Nume Prenume Grupa ---
--- Martie 2015 ---

-- Descriere --
Tema implementaza o tabela de dispersie (hash table) folosind functii
din ANSI C. Tema a fost compilata si testata cu succes pe ambele platform: 
Windows si Linux.

Implementarea a fost facuta conform enuntului precizat.
Codul a fost comentat si respecta regulile de coding style.
Programul are o structura modulara bazata pe functii ce pot fi
refolosite.
Pentru implementarea functiei de hash a fost folosita functia precizata
in enunt.

Solutia se bazeaza pe structurile node si hash_t, asupra carora se
exercita diverse operatiuni de modificare conform cerintelor.

Am folosit recomandarile de pe wiki, implementand diferite macro-uri
(eg. DIE, dprintf), ce au fost reutilizate periodic in functie de
necesitati.

-- Instructiuni compilare --
    - Pe Linux:
    make -f GNUmakefile
    - Pe Windows:
    nmake /f Makefile

-- Instructiuni rulare --
    ./tema1 <size> [<file1> <fileN>]
    ./tema1 <size> < file1

-- Listă fișiere --
    - tema1.c       - Program principal
    - hash.h        - Declaratia functiei de hash furnizata in enunt
    - hash.c        - Implementarea functiei de hash furnizata in enunt
    - util.h        - Declarația funcțiilor folosite pentru
      implementarea temei
    - util.c        - Implementarea functiilor folosite pentru
      implementarea temei
    - GNUmakefile   - Makefile pentru Linux
    - Makefile      - Makefile pentru Windows
    - README        - Fisier cu explicatii despre tema
